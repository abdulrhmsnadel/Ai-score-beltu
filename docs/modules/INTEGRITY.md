# Software & Data Integrity Engine

**Input:** authorized HTML URL.

**How it works:** parses external scripts for SRI metadata, validates SRI hash algorithm names, and detects HTTP resource references in an HTTPS document.

**Evidence:** external script URLs, SRI attributes, and mixed-content signal.
